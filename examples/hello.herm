# Hello World in Hermes (Sangam skin)

scheme greet(name):
    announce("Hello, " + name + "!")
    abandon truth

scheme main():
    result = greet("World")
    aahaan result:
        announce("Greeting successful!")
    thats_it:
        announce("Something went wrong")

main()
